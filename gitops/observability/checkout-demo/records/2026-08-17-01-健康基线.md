# checkout-demo 告警演练记录：2026-08-17-01 健康基线

> 记录时间：2026-08-17 23:51 CST（UTC+08:00）。只记录已执行且可验证的事实；未执行的告警分级、路由和故障演练不在本记录中推断。

## 1. 演练范围

| 项目 | 记录 |
| --- | --- |
| 时间窗 | 2026-08-17，约 23:45–23:51 CST |
| 目标 | 验证 GitOps 健康基线能形成 Deployment、Service、EndpointSlice、ServiceMonitor、Prometheus 与日志证据链 |
| 环境 / 集群 / 命名空间 | `lab` / `k3d-ai-lab` / `demo` |
| 服务 / owner | `checkout-demo` / `commerce` |
| 变更类型 | 健康基线部署与观测验证 |
| 故障注入 | 未执行 |
| Alertmanager webhook | 未触发；本阶段未增加新 PrometheusRule 或 Alertmanager route |

## 2. GitOps 与 Argo 审计链

| 节点 | Git commit SHA | Argo Application revision | Sync 状态 | 观察结果 |
| --- | --- | --- | --- | --- |
| 健康开始点 | `4e9328cc23a824aeec4e8932a158121a1a62bf26` | `4e9328cc23a824aeec4e8932a158121a1a62bf26` | `Synced / Healthy` | `ai-lab` 与 `checkout-observability-lab` 均为健康 |

## 3. 运行时证据

| 观察层 | 命令 / 查询 | 已验证结果 |
| --- | --- | --- |
| Deployment / Pod | `kubectl -n demo rollout status deployment/checkout-demo` | rollout 成功；Deployment `3/3 Available`，3 个 Pod 均 `Running`、`1/1 Ready`、重启数为 0 |
| Service | `kubectl -n demo get svc checkout-demo` | ClusterIP Service `10.43.165.226:8080`，selector 为 `app.kubernetes.io/name=checkout-demo` |
| EndpointSlice | `kubectl -n demo get endpointslice -l kubernetes.io/service-name=checkout-demo` | 3 个 endpoint，`ready=true`：`10.42.0.55`、`10.42.2.103`、`10.42.3.104` |
| 服务身份 | Pod labels | `service=checkout-demo`、`team=commerce`、`environment=lab`、`cluster=k3d-ai-lab` |
| ServiceMonitor | `kubectl -n demo get servicemonitor checkout-demo` | selector 正确，抓取 `/metrics`，间隔 `15s` |
| Prometheus target | `/api/v1/targets` | 匹配 3 个 target，均 `health=up`，无 `lastError` |
| HTTP SLI 候选 | Pod 内向 `http://checkout-demo:8080/checkout` 发 6 次请求 | 6 次 HTTP `200` |
| Prometheus metrics | `sum(checkout_requests_total{service="checkout-demo"})` | 聚合请求计数为 `6` |
| 错误指标 | `sum(checkout_errors_total{service="checkout-demo"})` | 聚合错误计数为 `0` |
| Logs | `kubectl logs` | 多副本产生结构化 `checkout_succeeded` 日志，带 `service`、`environment`、`revision=baseline` 与 `request_id` |
| Alerts | `ALERTS{service="checkout-demo",alertstate="firing"}` | firing series 为 `0` |

## 4. 结论与下一步

健康服务、流量、Metrics、Prometheus 抓取和结构化日志证据链已经成立。该结果不证明告警分级、路由、分组、抑制、去重或 Silence；这些机制尚未配置到 `checkout-demo`，应在下一阶段通过独立 Git 变更和可回滚演练验证。

## 5. 恢复与关闭判据

本记录为健康基线，不涉及故障恢复。基线后续可作为受控故障与 Git revert 的健康对照点：`4e9328cc23a824aeec4e8932a158121a1a62bf26`。
